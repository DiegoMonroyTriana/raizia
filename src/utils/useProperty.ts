import properties from '../../__mocks__/leads.json'
import transactions from '../../__mocks__/transactions.json'

const useProperty = () => {
  return {
    properties,
    transactions
  }
}

export default useProperty;
